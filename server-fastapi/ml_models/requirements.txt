# ML Models - Python Dependencies
# Install with: pip install -r ml_models/requirements.txt

# Core ML libraries
pandas>=1.5.0
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Visualization (optional, for training analysis)
matplotlib>=3.7.0

# Advanced models (optional, for better performance)
xgboost>=2.0.0

# Text processing (already included in scikit-learn, but listed for clarity)
# TF-IDF vectorization is part of sklearn.feature_extraction.text

# Optional: For BERT-based text similarity (much slower, but more accurate)
# sentence-transformers>=2.2.0
